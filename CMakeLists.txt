PROJECT(Mammoth)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
AUX_SOURCE_DIRECTORY(./src DIR_SRCS)
ADD_SUBDIRECTORY(lib)

MESSAGE("source_dir:${Mammoth_SOURCE_DIR}")
MESSAGE("binary_dir:${Mammoth_BINARY_DIR}")
MESSAGE("dir_srcs:${DIR_SRCS}")
